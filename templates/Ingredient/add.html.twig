{% extends "Admin/layout.html.twig" %}

{% block title %} {{ 'app.pagetitle.ingredient.add'|trans }} - {{ parent() }}{% endblock %}

{% block body %}
    <h3 class="mt-5">{{ 'app.title.ingredient.add'|trans }}</h3>
    {{  include("Ingredient/_form.html.twig") }}
    <p class="alert alert-danger mt-3"> {{ 'app.form.recipe.add.disclaimer'|trans }} </p>
    <p>
        <a href="{{ path('app_recipe_index') }}" class="btn btn-outline-secondary" id="return__button" >
            {{ 'app.form.ingredient.return'|trans }}
        </a>
    </p>
{% endblock %}