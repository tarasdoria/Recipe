<div class="well bg-light">
    {{ form_start(form, {'attr': {'class': 'form-inline justify-content-around my-3 py-3' },'method': 'post'}) }}
    {{ form_errors(form) }}
    <div class="form-group">
        {{ form_widget(form.username, {'attr': {'placeholder': 'app.grid.user.username'|trans }}) }}
    </div>

    <div class="form-group">
        {{ form_widget(form.email, {'attr': {'placeholder': 'app.grid.user.email'|trans }}) }}
    </div>
    <div class="form-group">
        {{ form_widget(form.search, {'attr': {'class': 'btn btn-primary'}}) }}
    </div>
    {{ form_rest(form) }}
    {{ form_end(form) }}
</div>