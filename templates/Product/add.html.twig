{% extends "Admin/layout.html.twig" %}

{% block title %} {{ 'app.pagetitle.product.add'|trans }} - {{ parent() }}{% endblock %}

{% block body %}
    <h3 class="mt-5">{{ 'app.title.product.add'|trans }}</h3>
    {{  include("Product/_form.html.twig") }}
    <p class="alert alert-danger mt-3"> {{ 'app.form.product.add.disclaimer'|trans }} </p>
    <p>
        <a href="{{ path('app_recipe_index') }}" class="btn btn-outline-secondary" id="return__button" >
            {{ 'app.form.product.return'|trans }}
        </a>
    </p>
{% endblock %}